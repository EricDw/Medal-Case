package com.challenge.medalcase.medals.samples

/**
 * Stands in for a real URL that would be used to retrieve an image from a remote source.
 * in a real application the URL would just be a [String].
 */
enum class ImageURL() {
    VIRTUAL_HALF_MARATHON_RACE,
    TOKYO_HAKONE_EKIDEN_2020,
    VIRTUAL_10_K_RACE,
    HAKONE_EKIDEN,
    MIZUNO_SINGAPORE_EKIDEN_2015,
    VIRTUAL_5_K_RACE,
}